{
  "dependencies": {
    "five-server": "^0.4.4"
  },
  "scripts": {
    "start": "npm-run-all --parallel server live",
    "server": "five-server --port=8080 --open=index.php",
    "live": "browser-sync start --proxy \"localhost:8080\" --files \"**/*.{php,html,css,js}\" --no-notify"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "browser-sync": "^3.0.4"
  }
}
